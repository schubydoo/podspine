# cargo-deny configuration (TAD §6.4 supply-chain gate).
# Tune the license allow-list as real dependencies land in Sprint 1+.

[advisories]
version = 2
yanked = "deny"
# proc-macro-error (RUSTSEC-2024-0370) is unmaintained but reaches us only as a
# BUILD-TIME proc-macro dep via maud_macros -> maud -> podspine-ui; maud 0.26
# (latest) still uses it and there is no safe upgrade. It is not a runtime
# dependency, and the advisory is "unmaintained", not a vulnerability. Revisit
# if/when maud drops proc-macro-error.
ignore = ["RUSTSEC-2024-0370"]

[bans]
multiple-versions = "warn"
wildcards = "deny"
# Internal workspace crates are path deps with no version requirement — a
# "wildcard" to cargo-deny, but expected and correct. Only EXTERNAL wildcard
# deps are the smell this rule is meant to catch.
allow-wildcard-paths = true

[licenses]
version = 2
confidence-threshold = 0.9
# Silence "license was not encountered" for the forward-looking allow-list below
# (we list licenses we'll hit once real deps land, before they're in the tree).
unused-allowed-license = "allow"
allow = [
    "AGPL-3.0-only",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "MIT",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Unicode-3.0",
    "Zlib",
    "MPL-2.0",
    "CC0-1.0",
]

[sources]
unknown-registry = "deny"
unknown-git = "deny"
