---
default: minor
---

On the `/subscribe` page, put each podcatcher's QR code behind its own collapsible section (an exclusive `<details>` accordion, no JavaScript), so only the app you expand is on screen to scan and a phone camera can't lock onto a neighbouring app's code
